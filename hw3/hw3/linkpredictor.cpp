#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include <stdio.h>
#include "ActorGraph.hpp"
using namespace std;

int main(int argc, const char** argv) {
/*

	if(argc != 5) {
		cerr << "Incorrect number of arguments." << endl;
		exit(-1);
	}
	ActorGraph a;
	a.loadFromFile(argv[1]);
	remove(argv[3]);
	remove(argv[4]);
	if(!a.linkpredictor(argv[2], argv[3], argv[4])) {
		exit(-1);
	}
*/
}
